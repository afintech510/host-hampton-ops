# Host Hampton Ops — Master Plan

## Phase 1A: Core Orchestrator + Agents ✅ COMPLETE

- [x] HAMPTON orchestrator (Express + WebSocket + Redis pub/sub)
- [x] Redis infrastructure
- [x] SOC agent (social listening, comment scanning)
- [x] COPY agent (content writing → content_library)
- [x] IMAGE agent (Replicate image gen — working)
- [x] LIST agent (email list management)
- [x] OUTBOUND agent (email/SMS dispatch + Resend integration)
- [x] Docker Compose on VPS (5.161.88.134)
- [x] Cloudflare SSL (Full Strict) with Origin Certificate
- [x] CORS fix (Express-only, no nginx CORS headers)
- [x] Dashboard at app.hosthampton.com (VPS Docker → nginx proxy)
- [x] Approval gate (AUTO_EXECUTE / DRAFT_AND_SHOW / ALWAYS_ASK)
- [x] History tab (completed/failed task log)
- [x] Content Library tab (content_library browser)

## Phase 1B: Immediate Fixes + Unblocking ✅ COMPLETE

- [x] Deploy latest commits to VPS
- [x] Verify approve/reject buttons work
- [x] Verify Content tab populates
- [x] Unblock IMAGE agent

## Phase 1C: Testing & Validation ✅ COMPLETE

- [x] Test approval workflows end-to-end (DRAFT_AND_SHOW + ALWAYS_ASK)
- [x] Populate agent_memory (brand voice, services, campaigns — 10 entries seeded)

## Phase 2: INTEL Agent + Resend ✅ COMPLETE

- [x] INTEL agent built and deployed (GA4 + internal metrics + Claude reports)
- [x] GA4 OAuth2 credentials working (live data: 534 sessions/wk)
- [x] OUTBOUND Resend email sending live
- [x] Dashboard Reports tab (surface analytics from agent_memory)
- [x] nginx DNS resolver fix (127.0.0.11 valid=5s)

## Phase 3: Website Redesign + Booking ← CURRENT

- [x] Next.js 14 website scaffolded (20 pages, build succeeds)
- [x] Brand design finalized (Dusty Blue/Blush/Ivory, Libre Baskerville + Poppins)
- [x] SEO foundations: sitemap.xml, robots.txt, schema.org LocalBusiness JSON-LD
- [x] migration_004 written (bookings + website_content tables)
- [x] Claude Code templates installed (frontend-dev, backend-architect, SEO skill)
- [x] **Run migration_004 in Supabase** — bookings + website_content tables
- [x] **Create Stripe webhook** → whsec signing secret configured
- [x] **Wire Stripe deposit checkout** into /book page ($99 default deposit)
- [x] **Google Calendar integration** (availability + date blocking + write-back)
- [x] **Deploy website to VPS** (Docker + nginx staging.hosthampton.com)
- [x] **Event ticketing system** — migration_005 + events/sessions/tickets + admin dashboard
- [x] **Universal Calendar Module** — 18 booking types, variable deposits, GCal sync
- [x] **Pricing database** — pricing_items table (87 items), /api/pricing, /party-menu page
- [x] **Visual redesign** — dusty blue gradient, glass nav, bronze accent sections
- [x] **Homepage theme tiles** — image slider, mini party pricing, two-tier card, scroll-to-panel, updated CTAs
- [x] **kids-party-menu** — DIY rental section (collapsible) + food/balloon qty adjusters
- [x] **Book page UX** — show form/themes immediately on party type select (no timeslot wait)
- [x] **DNS cutover**: www.hosthampton.com from Squarespace → VPS ✅ (2026-03-07)
- [x] **Admin UI modernization**: Sidebar nav, Dashboard tab, KPI cards, glass login
- [x] **Financials system**: financial_transactions table, CSV import (GoDaddy/Squarespace/HoneyBook), Stripe auto-record, server-side aggregation, timeframe selector, category filter, inline category editing
- [x] **Email sequences engine**: 4 active sequences, cron processor, 13 enrollment points
- [x] **Update Stripe webhook URL** to `https://www.hosthampton.com/api/webhook` ✅ (we_1T7ckv02uXWznKaWMiPeXCCf)
- [x] **Run migration_005 in Supabase** — event ticketing live ✅
- [x] **CM Cheer Fundraiser**: Order form at `/cm-cheer`, organizer dashboard at `/cm-cheer/orders`, Supabase DB + RLS, local product photos, mobile-optimized, notes field, CSV export
- [~] **Migrate remaining hardcoded prices** to pricing_items table — Mobile tiers, studio rate card and the party-planner constants moved in migration 036 behind `lib/pricingCatalog.ts` (2026-09-11)
- [x] **OG image support for event pages** — dynamic openGraph metadata from DB images
- [x] **SEO optimization pass** — **done 2026-09-12, `docs/seo-pass.md`. No migration (043 still free), no new env.** Every claim was measured on the live site before anything was changed, and the brief's own starting facts did not survive that: of the "twenty pages with no metadata", **nine inherit real metadata from a sibling `layout.tsx` and two are not pages at all** (`/classes` and `/party-add-ons` are bare `redirect()` calls). The real damage was somewhere the count could not see. **`/book` — the primary conversion page, sitemap priority 0.8, linked from the nav and footer of all 69 URLs — was serving TEN CHARACTERS of HTML: `Loading...`.** It *was* the client component (`'use client'` + `useSearchParams()` + one `<Suspense>`), and a client component that reads `useSearchParams()` bails out to its Suspense FALLBACK during a static prerender, so the build happily labelled it `○ Static` and prerendered the fallback. Google renders JS on a second pass and would eventually see the form; **the AI crawlers `robots.ts` goes out of its way to welcome by name — GPTBot, ClaudeBot, PerplexityBot — do not execute JavaScript at all**, so to every one of them the booking page was blank. `page.tsx` is now a server component carrying the `<h1>`, the copy and six internal links, with the booking form unchanged beneath it in the same Suspense: **10 chars → 1,035, and one `<h1>` where there were none.** **Structured data validated rather than admired, and four defects found, two of them publishing prices.** (1) **Every `Event` rich result on the site was invalid** — `"startDate": "2026-10-09T7:00 PM"` is not ISO 8601, because `event_time` is free text with **seven** live shapes (`'7:00 PM'`, `'10:00 am'`, `'6:00'`, `'10'`, `'1'`…) that the page concatenated blind. No Host Hampton event has ever been eligible for Google's event listings. `lib/eventSchema.ts` parses only the unambiguous forms and **DROPS the rest to a date-only `startDate`** (valid ISO 8601, valid for Event) — rule 15, because a time four hours wrong in an event listing sends a family to a closed door. The UTC offset is taken from `Intl` **after verifying full ICU inside the running container**, since a small-ICU Node answers UTC and would shift every event by four hours. (2) `availability` was hardcoded `InStock` though `available_tickets` is maintained, and a single `Offer` published the base $45 while the page and `/events` both show "from $35" — now `SoldOut`-aware and an `AggregateOffer` over the variants. (3) `/fundraiser` published an `Offer` with `priceCurrency` and **no `price`** — invalid, rejected wholesale. (4) **`/first-birthday-parties` published `price: '850'` against its own visible table of $650 / $800 / $1,045** — the schema is now DERIVED from the hoisted `PACKAGES` constant the page renders (rule 11). **40 of 69 URLs shared as a blank card**: Next merges metadata SHALLOWLY, so every page that declared its own `openGraph` replaced the root layout's whole object and lost `images` — `OG_DEFAULTS` now spreads into all 20 blocks and **a test walks `src/app` and fails if a new page reintroduces the gap**. 39 titles over 60 chars and 56 descriptions over 160 (measured entity-decoded; the raw-HTML first pass over-counted), with the 26 town titles at 85–96 — `townMeta()` moved into `lib/locations.ts` so the budget is asserted across all 26 at once. `noindex` (not robots.txt — a disallowed URL is never fetched, so the directive is never read) on the admin tree, the portal, `/plan`, `/review`, `/checkin` and the confirmations; `robots.ts`'s disallow list got SHORTER, plus `/admin` without the trailing slash, which `/admin/` never matched. 19 `<Image fill>` had no `sizes` and were pulling the **3840px** variant into a card; the homepage LCP tile was `loading="lazy"`. The footer's `<h4>` headings broke the outline on all 69 pages. `/studio-rental` was linked from two pages sitewide. `/es/party-room-rental` rendered under `<html lang="en">` while its own hreflang said `es`. **The one agent-path change:** `website_content.structured.jsonLd` is rendered verbatim and `website_content` is written by the COPY agent and the town-drafts cron — no writer sets it today, but the READER accepts it (rule 5), so `carriesPublishedPrice()` screens it at any depth with a `WeakSet` rather than a depth cap (a depth cap is a bypass) and drops it loudly. **1284 tests (was 1154), all green; 0 app-code tsc errors; `next build` clean with `/book` still `○ Static`.**
- [x] **Agent content pipeline**: COPY → website_content → ISR → published pages — **done 2026-09-12, `docs/content-pipeline.md`. No migration (043 still free), no new env.** The plumbing half-existed and had never been switched on: all five `website_content` rows were `created_by = 'manual'`, the only content rows in `marketing_ledger` were three hand transitions on 2026-08-17, and **the weekly town-drafts cron appears 0 times in 10 days of nginx logs** against 805 for `agent-dispatch` — it is not scheduled at cron-job.org. (The brief's claim that there was no admin UI was also wrong: `MarketingTab.tsx` has had a full pipeline section all along. What it lacked was any way to CORRECT a row.) **The four English drafts can never render, and nothing said so.** `[...slug]` is a catch-all and Next gives a static segment precedence over it, so `first-birthday-parties`, `communion-party`, `fundraiser` and `cm-cheer` — each of which has a hand-built `app/<slug>/page.tsx` — would go `published` in the graph, write a ledger row, turn the panel badge green and enter the sitemap while a visitor saw the static page. They also hold no `structured` content at all: dead data from 2026-02-21, when the DB renderer was the plan. The one PUBLISHED row is Spanish, and that is exactly why it works — no static route begins with an `es` segment. `lib/content/slugSafety.ts` now decides this from the same data the renderer uses, **before the Claude call** (a draft under a dead slug is budget spent on an unreachable page), at the publish gate (409, naming the page in the way; non-publish transitions are NOT blocked so a dead row can still be archived), in the sitemap, and as a banner + disabled Publish button in the panel. A test walks `src/app` and fails if a page route is missing from the pattern list, asserted **one-directionally** because a missing entry is what calls a shadowed slug safe. **The renderer executed agent-written HTML.** `structured.sections[].html` and `body_html` went to `dangerouslySetInnerHTML`, and `createTownServiceDraft` `JSON.parse`d Claude's reply and inserted `sections` **wholesale** — the `DraftResult` annotation was a cast, not a parser — so a model answering `"html"` instead of `"text"` had its markup rendered verbatim, **first in the admin PREVIEW MODAL, where the reader holds an authenticated `hh_admin` session**. Fixed at the READER as well as the writer (rule 5: `body_html` is a column and the next writer will not remember), and fixed by making DB content TEXT rather than by adding a sanitiser — a sanitiser is a list of things you thought of, and no live row has ever used either field. **The SEO budget lived only in a prompt**: the live Spanish row carried 165 characters and `first-birthday-parties` 171. `lib/content/draftNormalize.ts` is the writer-side parser — importing `MAX_TITLE_CHARS`/`MAX_DESCRIPTION_CHARS` and stating them IN the prompt from the same constants (rule 11), trimming on a word boundary while KEEPING the brand suffix, bounding every array, flattening U+0085/U+2028/U+2029/C1/bidi, refusing a reply with no title or no body, and reporting every trim to the ledger and the panel (rule 10). **On ISR: route-level `revalidate` does not work on this route, and that was MEASURED** — setting it leaves `[...slug]` as `ƒ (Dynamic)` in the build table, because `lib/supabase.ts` sets `cache: 'no-store'` on every fetch and a no-store fetch opts its route out of static rendering. (That applies to every Supabase-reading route in the app.) It is also a catch-all, so its key space is whatever a scanner requests. The cache is therefore at the DATA layer: one entry for the published index (a path not in it costs no query and no cache entry, and it replaces the per-request hreflang query outright), one per row that exists, flushed **by tag** on every transition and edit so a publish is live at once. `Lookup` is `found | absent | unavailable` — a failed read is a 500, not a 404, because 404ing every published page at once tells Google the content was deleted (rule 12). Also: `reviewed_by` was the literal `'admin'` and now names the person; `content[0]` was read as text and is now the first TEXT block (rule 1); and `PATCH /api/admin/marketing/content` gives the reviewer the missing half — title, description, slug — as a **whitelist that excludes `status`**, so it cannot become a second ungated publish door. **Driven end to end in production:** a real Southampton draft ($0.0039) taken to published and verified live with correct metadata, JSON-LD and a sitemap entry with **no duplicate URLs in the file**; the weekly cron run by hand in both auth forms (a wrong secret 401s) drafting four more towns, all inside budget, all waiting for Allie. **The cache proved by CHANGING a value (rule 6):** a title changed directly in Postgres did NOT appear (the cache is real), then the same field changed through the route appeared on the next request (the flush works) — both halves, not one. **Then attacked (rule 8):** a `published` row carrying scripts in `body_html` and `sections[].html`, `onerror` images, `javascript:` in `featured_image` and `gallery[]`, and a `jsonLd` publishing `offers.price: 399` on a MOBILE party was inserted straight into production Postgres past every write guard. The price screen dropped it, both script bodies went, the visible DOM was clean — **and two readers were found unscreened, both mine**: `buildJsonLd` read the RAW `structured.faq` (publishing a `<script>` tag as a Question's text to Google — escaped and inert, but rule 11's failure shape: screened in one reader, not the other) and `openGraph.images` published the raw `featured_image` three lines from where the `<img>` refused it. Both fixed, redeployed, probe re-run: zero payload markers, no price, `og:image` fell back to the default, the Question node read `"Question?"`; row deleted and its URL confirmed 404. **A stale price found on the way:** `/es/party-room-rental` advertised *"Desde $450 por 3 horas"* as its meta description while the English page and `pricing_items` both say **$475** (weekday) / $600 (weekend) — corrected through the new route, which also took it from 165 to 128 characters and closed the last over-budget snippet from the SEO pass. **1437 tests (was 1284), all green; 0 app-code tsc errors; `next build` clean with `/book` still `○ Static`.**

## Phase 3B: Communications Infrastructure — reminder engine repaired 2026-09-12; two settings open

> **The reminder engine had never run — not "was never scheduled", but *could
> not have worked if it had been*.** Reviewed by link 10 on 2026-09-12
> (`docs/reminder-engine-review.md`, **migration 044**). `scheduled_reminders`
> held zero rows in five months because the table refused every insert the code
> made — `reference_id` was `uuid` and the enqueuers write a booking_ref
> (22P02), and the `reference_type` CHECK had no `'event'` label (23514) — and
> not one writer read the error. The sender had no claim, marked non-sends as
> `sent`, and never re-read email consent at send time; `event-reminders` texted
> ticket holders without ever consulting `contacts`. All fixed, deployed, and
> driven in production. **It still does not fire: none of the three cron jobs is
> scheduled** (needs-Adam 18), and the 18 real upcoming bookings that were
> silently given no reminders are a backfill decision for Adam (needs-Adam 19).


- [x] Brevo REST API wrapper (bulk email campaigns, contact sync)
- [x] Twilio REST API wrapper (SMS/MMS with opt-in check)
- [x] SMS templates (event reminders, booking reminders, confirmations)
- [x] Email templates (reminders, newsletters, marketing)
- [x] Reminder engine (auto-enqueue on ticket purchase + booking) — **shipped broken, repaired 2026-09-12 (migration 044)**, see the note above
- [x] Cron routes (send-reminders, send-campaigns, draft-newsletter)
- [x] Webhook handlers (Brevo bounces/unsubscribes, Twilio STOP/HELP/inbound)
- [x] TCPA/CTIA/10DLC compliance (Privacy Policy, Terms of Service, consent checkboxes)
- [x] Admin Contacts tab (list/filter/search, detail, edit status/notes/opt-in)
- [x] Admin Campaigns tab (CRUD, Brevo send, draft newsletter, process reminders)
- [x] Webhook bug fixes (interaction_type → type in Brevo + Twilio handlers)
- [x] Marketing consent checkboxes on all lead/contact forms
- [x] Contact export for Google Ads / Meta retargeting (CSV download)
- [x] 178-test suite across 15 suites (all API routes covered)
- [x] **Configure Brevo — MEASURED DONE 2026-09-12** (link 7). The box had been open since the phase shipped; nobody had looked. Probed read-only against the live Brevo API with the container's own key: account **200** (`Host Hampton`, free plan); `BREVO_DEFAULT_LIST_ID=3` is **"Host Hampton Leads" with 944 subscribers**; the domain `hosthampton.com` is `authenticated: true, verified: true` via DNS on Cloudflare, so the **DNS records are in place**; and the **marketing webhook is already registered** at `https://www.hosthampton.com/api/webhooks/brevo` for `hardBounce`, `softBounce`, `delivered`, `click`, `opened`, `unsubscribed` — our own route, with the events our handler reads. `BREVO_SENDER_EMAIL` is `info@hosthampton.com`, which is **not** an individually-verified sender but **is** at the authenticated domain, which is what Brevo requires. **Unproven, deliberately:** no send was made — the only list is 944 real people and a campaign is not a test. The transactional webhook is unregistered, which is correct: transactional email goes out through Resend, not Brevo.
- [ ] **Needs Adam — the Twilio inbound webhook points at somebody else's system.** `TWILIO_PHONE_NUMBER` is **+1 844 624 0400**, and that number's `SmsUrl` in the Twilio console is `https://losrkjvrcambvgijfism.supabase.co/functions/v1/receive-sms-webhook` — a **different Supabase project** from Host Hampton's (`ychnlroczjhwimouecxz`), with `StatusCallback` pointing at a Google Cloud Function in a project called `twiconnect-257c6`. So `/api/webhooks/twilio` exists, is tested, and **has never received a single message**. Scope, measured rather than assumed: the **agent's** booking conversations are unaffected — `reviewLoop.ts` and `sendApproved.ts` call `sendSMSViaQuo` directly, bypassing the `SMS_PROVIDER` router, and Quo is a different number with its own working webhook. What is affected is **Phase 3B's reminder and campaign SMS**, because `SMS_PROVIDER=twilio`: those go out on the Twilio number and every reply — **including STOP** — lands in the foreign endpoint. Twilio still honours STOP at the carrier level, so nobody is messaged against their wishes; but our `contacts` opt-in state never learns about it, so the DB keeps a stopped number marked opted-in and the reminder engine keeps trying it. **Not changed by the build session on purpose:** repointing the inbound webhook of a live phone number is outward-facing and would break whatever is consuming it today, which is Adam's call and not a technical one. When he confirms, it is one API call — `POST /2010-04-01/Accounts/{SID}/IncomingPhoneNumbers/{NumberSID}.json` with `SmsUrl=https://www.hosthampton.com/api/webhooks/twilio`.
- [x] Set up external cron service (cron-job.org, every 15 min) ✅ (4 jobs running)

## Phase 3C: Booking Agent ← CURRENT (plan: `docs/booking-agent-plan.md`)

- [x] Phase 0 — `inquiry_drafts` schema (migration_028), classifier + required-info gate (`lib/inquiryDrafts.ts`, 24 tests)
- [x] Phase 1 — lead trigger → Claude draft → SMS to reviewers (stub send); `lib/agent/{config,events,voice,reviewLink,draftInquiry}.ts`, `/api/cron/agent-dispatch`, `/review/[token]`, admin Inbox tab + `/api/admin/agent`; migrations 032 + 033. **Live 2026-09-11** (reviewed and fixed in 912111c: sweep re-draft loop, claim reaper, budget re-queue, money guardrail).
- [x] Phase 2 — SMS review loop (SEND / EDIT / TEST / CANCEL) + real send via Resend + Quo; Quo webhook fail-closed + dedupe + unknown senders become contacts; `lib/agent/{reviewers,reviewLoop,sendApproved}.ts`, `inquiry_draft` as a GATED graph entity, 2h business-hours nudge, admin Send/Test; migration_034. **Deployed 2026-09-11**, then reviewed and fixed in 361c57d: the parser ate a trailing number out of a revision note, a quoted iPhone reply was parsed as intent, the send had a double-send window (per-channel claim now precedes the send), an impossible channel stranded a draft at `approved`, and an approval naming an out-of-context draft now confirms once.
- [x] Phase 3 — permanent Gmail ingestion + triage. `lib/gmail.ts` (read + label only, its own OAuth grant, no send scope), `/api/cron/gmail-sync` (history checkpoint with a bounded `newer_than:2d` fallback, both directions ingested, `?backfill=1` for the 12-month corpus pull), `lib/agent/triage.ts` (free sender filter → Haiku, schema-enforced, category-gated so an injected body cannot make itself actionable), dispatcher wiring, cron-job.org job 8429172. No migration needed — 032 already had `gmail_sync_state`. **Reviewed and fixed 2026-09-11 in a2dd707**: `sent_at` was ingestion time so the backfill made every outbound row look newer than any inbound question (the stand-down check silently over-firing); the history checkpoint had no poison-message escape and could stall the mailbox permanently; an injected email body reached the *draft* prompt un-fenced, now fenced plus an output-side `containsForeignContact()` park; admin "Draft now" had no age check over 270 backfilled messages; and the Handled label was split into Seen (ingestion) + Handled (drafted). Plus a sixth, the one that mattered most: an injected email body could steer a QUOTE-path draft into a fabricated promise ("your deposit is waived", a $1 total) that no existing guardrail could see — `containsMoney()` only ever ran on info-gather, and a waived deposit names no figure. `containsFabricatedTerms()` parks it. Also: `bookings.contact_id` never existed, so thread→plan linking had never worked — added in migration 035 §9.
- [~] Phase 4 — every lead is a Party Plan. **Items 1–3 deployed 2026-09-11**: migration 035 applied (status CHECK with `lead`/`quoted`, `party_type` backfilled over all 41 rows, `source`, `first_touch_event_id`, `invoice_number` + `invoice_number_seq` from 444124-000116, `booking_pay_links`, check/other payment methods, and the four NOT NULLs a lead can't satisfy relaxed behind a reachability CHECK and a `scheduled_fields` CHECK that re-imposes them past `quoted`); `lib/plan.ts` (`buildPlanSnapshot` / `planTotals` / `writeLineItems` / `ensureLeadPlan` with the 30-day reuse rule); all seven intake routes create or reuse a plan **before** recording the event so the dispatcher's booking sweep can't double-draft; `quote/save` writes real `booking_line_items`; the three divergent `quote_snapshot` writers collapsed onto the shared builder; 34 new tests.
  - **Items 4, 5 and 6 deployed 2026-09-11** (plan §14). **Item 4** — migration 036 (data only, 46 rows across four new `pricing_items` categories) + `lib/pricingCatalog.ts`: the Mobile tiers, studio rate card and the planner's inline constants are DB rows keyed by `metadata->>'catalog_key'`, with the pre-036 constants kept as a per-field fallback so a broken table renders today's prices rather than $0. `studioRentalRateWith(rates, …)` keeps the rate engine synchronous for the two client components that re-price interactively; `lib/mobilePricing.ts` deleted; the three mobile marketing routes gained `revalidate = 3600` because they prerender at build time where `SUPABASE_URL` does not exist. Surfaced (not fixed — Adam's call) that the planner's $400 mobile base and the published $500 Entry tier are two different products that had never been in the same file. **Item 5** — pipeline header + `party_type` filter + live stage counts in `PartiesTab`, `lib/pipelineStages.ts` shared with the API, and "Draft reply with agent" (`lib/agent/manualDraft.ts`) for phone leads, which enqueues a real event and takes it through the dispatcher's own `claimInboundEvent()` behind four anti-double-draft layers. Fixed: the route's `event_type` allowlist was showing **32 of 41** party rows, hiding four `room-rental` studio bookings, three `'Kids Birthday Party'` rows and both mobile leads. **Item 6** — `mergeInquiry()` so the draft node evaluates the plan *and* the message (it was reading `event.parsed` alone despite the event carrying a `booking_id`, so a returning customer was re-asked for a date we already had), plus `lib/agent/extractPlanFields.ts`: a prose reply is read for the missing fields only, validated locally, written blanks-only, and the gate re-run. 77 new tests (703 total). Then two fixes from the Phase 3 review session handoff: extraction had opened a prompt-injection hole through the *structured* half of the draft prompt (`contact_name` and the new free-text-date hint are unfenced, and a newline in either forges a section header — `flattenToOneLine()` now flattens at both the extraction source and the prompt boundary, which also closes the pre-existing form-field path), and `bookings.first_touch_event_id` is finally written via `linkFirstTouchEvent()`, guarded fill-once because the plan is necessarily created before the event exists. **Reviewed adversarially 2026-09-11** (plan §16): the ordering contract held across all eight writers and the manual-draft precheck matches the partial unique indexes exactly, but six things around them were wrong — `findOpenPlan` built a PostgREST `or()` out of raw customer input (confirmed live: a crafted email adds a disjunct and returns a stranger's bookings, which `enrichPlan` would then write onto), phone matching was raw string equality so the person who texts then fills in the form got a SECOND plan and a second text, a third prompt-injection door ran through `classifyPartyType`'s `reason` quoting customer-written `package_type` into the trusted half, the `<their_message>` fence could be closed by the data inside it, `applyExtractedFields` wrote unguarded after its own blankness read, and the "a fallback is never cached" rule was false for a row that exists and prices at 0. **Remaining in Phase 4**: the planner product switch (load any plan by ref, make the studio/mobile sections a real product selector) — folded into Phase 4.5's plan panel, which is where it gets used.
- [ ] **Mobile pricing rework** (Adam, 2026-09-11) — spec in plan §15, **blocked on Adam's numbers**. Stop publishing a firm mobile rate card; base price as a table by guest band (10/15/20/25) with craft stations priced per band; location as a real rule, not "a modest mileage charge". **Live gap worth knowing: the site publishes $500/8 and $750/12 ($62.50/child) while Adam's target is $850/10 ($85/child), and the planner charges less still — every mobile booking today is underpriced against intent.** The catalog from migration 036 is the right substrate: band grids fit in `pricing_items.metadata` with no DDL.
- [x] **Incident 2026-09-11 — a lead produced no reviewer text** (plan §18, `cc09115`). Eleonore's mobile-party lead created its plan and event correctly, then went silent. `containsMoney()` held a draft containing no figure: month-name dates were unmasked, so "pricing together for **Oct 10 or 11**" left "or 11" bare next to a money word — every lead offering two candidate dates would have hit it. And **parking was silent**: the reviewer SMS is skipped for `status='drafted'` and the nudge only watches `sent_for_review`, so a held draft was never mentioned again by anything. Both outcomes text now (`parkedSmsBody`, deliberately without "Reply SEND", since `drafted → approved` is a legal edge). Also fixed a mock missing `.is()` that made `agentDispatch.test.ts` pass in the morning and fail 16 tests in the afternoon — the older 734/735 and 770/771 counts were morning numbers; current is **781/782**. Lead re-drafted as `HH-2026-0208`, awaiting Adam. **Rule: a guardrail that stops something must also say that it stopped it — silence is what success looks like.**
- [x] **Per-user admin login** (`admin_users`, migration 038) — **deployed 2026-09-11**, plan §19. The prerequisite Phase 4.5 and Phase 5 were BOTH blocked on. `adminAuth.ts` grew from 10 lines to a real session: the shared-password Bearer path is untouched (all ~56 admin routes keep `isAdminAuthorized(req)` and Adam can always get in), and a signed HttpOnly `hh_admin` cookie was ADDED on the same HMAC pattern as `portalAuth.ts`, with the 7-day expiry inside the signed payload so a client cannot extend it. Two consequences, which were the whole point: `advance()` now gets a real actor (`admin:allie@…` instead of the anonymous `'ADMIN'`), and a **server component can finally recognise an admin** — so `/plan/[ref]/summary` is natively admin-viewable and nobody opens a customer's portal link to read an invoice. Rows seed UNCLAIMED (no password hash in the repo, ever); first sign-in sets a scrypt hash and REQUIRES the shared password as proof, because /admin is a public URL and otherwise anyone who guessed `allie@…` could claim her account. `isAdmin` is unchanged and unwidened: a session NAMES an admin, it does not create one. 41 net new tests (823 total, 822 passing — the one failure is the inherited `smsReviewRequest` WIP).
  - **Finished 2026-09-11 (migration 039)**: 038 created the table but seeded **one** row, `adam@easternbuilding.supply` — so the feature could not yet do what it exists for, because **Allie had no account** and every approval she made would still have been the anonymous `'ADMIN'`. 039 seeds `hosthampton295@gmail.com` (Allie) and `adam@benchworksai.com` (Adam), unclaimed on 038's terms. Also closed the last hardcoded ledger actor: `/api/admin/parties/[id]`'s "Draft reply with agent" passed a literal `'ADMIN'` into `draftForBookingByHand`, so the one admin action that *starts* a customer conversation was the one still recording nobody — it now passes `adminActorId(req)`. And the UX dead end Adam raised: "HH-2026-0208 is already open for this plan, handle that one first" named a draft and gave no way to reach it; the refusal now carries `reviewCode` as a **field** (not scraped from the prose) and PartiesTab renders "Open HH-… in Inbox →", which switches tabs and scrolls the draft into view highlighted. 5 new tests (826 total, 825 passing — same inherited failure). **The shared `ADMIN_PASSWORD` is NOT yet retirable: it is the proof required to claim an account, and no account has been claimed yet.**
- [x] Phase 4.5 — **Lead Thread Workspace** — **deployed 2026-09-11**, plan §20. **The test suite is now 898/898 GREEN (`16a8703`) — the long-inherited `smsReviewRequest` failure was not WIP and not pre-existing-and-unownable; it was a constant declared in two files, and chasing it once beat documenting it for three sessions.** `/admin/lead/[ref]` is Allie's lead surface: one page per lead, resolving a booking ref, a review code from an SMS, or a draft UUID, so she never has to know which identifier she is holding. **`lib/agent/threadTimeline.ts`** assembles the thread from the five tables that already exist (`ingested_messages`, `inquiry_drafts.revisions[]`, `marketing_ledger`, `booking_payments`, `contact_interactions`) and creates **no new storage** — a sixth table would be the one row whose provenance nobody could explain. Messages sort by `sent_at`, not ingestion time (the 12-month Gmail backfill would otherwise put 2024 email at the top of a live thread), an absent anchor **skips** its query rather than issuing an unfiltered one, and a source that fails to load is reported rather than rendered as an absence that reads exactly like "nothing happened". The **chat composer** posts `action: 'revise'` to the existing `/api/admin/agent`, which calls the **same `redraftForReviewer()` the SMS loop calls** with the same record-the-note-before-spending ordering — one re-draft path, so the two surfaces cannot drift into only one of them still checking for fabricated terms. **Tone chips** (`warmer` · `shorter` · **`mom-to-mom`** · `less salesy` · `more specific on logistics` · `match my last message`) are authored constants in `lib/agent/tonePresets.ts`, and an unknown chip id is **dropped, never echoed** — `reviewer_note` is interpolated into the draft prompt as a *trusted* owner instruction, and the composer is a brand-new writer of that field. Inline SMS/email editing with real segment marks: `lib/smsSegments.ts` does the carrier's arithmetic, not `length / 160`, because one curly apostrophe — which is what a model writes for "kid's" — flips the whole message to UCS-2 and drops the limit to 70. **Approve & send** names the recipient in the confirm dialog and runs approve-then-send as two calls where a failed approve does **not** proceed to a send. Plan panel: pipeline header off the shared `lib/pipelineStages.ts`, product selector, inline plan fields through a **whitelist** `PATCH /api/admin/lead/[ref]` that excludes every money column and `status` (totals are derived, the stage moves through `advance()`), and a plan edit marks any open draft **stale** in the ledger and says so in both the panel and the thread. Every write carries `adminActorId(req)`; no literal `'ADMIN'`. **`/review/[token]` stays read-only** and gained only the timeline, a countdown and one "Open in Host Hampton →" deep link — plus the **7-day TTL** (`lib/agent/reviewLink.ts`), measured from the row timestamp the token is minted alongside, with an admin `edit` now **rotating** the token so a typo fix cannot silently grant a forwardable link another week; an expired link is a 404, like a wrong one, because "this link expired" confirms the draft exists. An unknown mint time counts as expired — the safe failure direction. **No migration** — 040 stays free for the learning loop. Entry points added from the Parties tab and the Inbox. 44 new tests (870 total, 869 passing — same inherited `smsReviewRequest` failure).
  - **"Send quote" finished the phase (`c19bb4a`), and production found a bug in it (`a1404b5`).** The button reuses the existing `draft_with_agent` action rather than a second endpoint, because that action already carries the three anti-double-text layers. The real gap was the silence around it: whether a draft comes out priced or as an info-gather reply is `evaluateInquiry()`'s call, so the route now returns the **same evaluation the draft node runs** and the panel says which is about to happen ("Ready to quote" vs "Still missing: date, guest count — a draft now would ask for those, not price it"), with the button's label following. Reachability (no email AND no phone) is reported separately, since it blocks the button entirely. Live it earned itself at once: Natalie's rental is quote-ready, Ashley's party is one field short, Eleonore's is four. **The bug:** `HH-2026-0208` came back as a lead with *no plan row* — it has one. A transient Supabase failure on the booking read was being discarded, so the route answered `booking: null`, which the panel renders as the confident sentence "No party plan row for this lead yet". Same rule as the held draft that notified nobody: **an absence reads exactly like a fact, so a failure has to say it failed.** `resolveLead` now separates "found nothing" from "could not tell" — a total failure is a 503 not a 404, PATCH refuses rather than implying you should make a second plan (the duplicate-plan problem `9fedd91` had just fixed), and a dangling booking reference is flagged while a draft with genuinely no `booking_id` is not. **Generalised: when a lookup can fail, the caller needs three outcomes, not two.** 887 tests, 886 passing.
  - **The Inbox queue became triageable, and found a live hazard on its first run (`cf6ddcc`, `a1e8e34`).** It rendered `created_at` as an absolute timestamp and sorted newest-first — neither is usable, and newest-first buries the neglected lead by construction, which is how §18's lead sat in that list while being missed. `lib/leadWaiting.ts` now states elapsed time in words with an escalating colour (grey inside the 2-hour nudge window, amber after it fires, red past a day — the honest threshold, because the nudge fires **once**), sorts longest-waiting first, and names the customer instead of only a review code. The clock prefers `sent_for_review_at` so a re-draft is not aged unfairly, falls back to `created_at` for a HELD draft (which never gets one), and an unreadable clock shows no badge and sorts LAST rather than displacing a real one. **What it found:** Shannon Ballantyne's four duplicate plans had been cleaned up — three cancelled — but the **drafts** on two of them were still open and looked like ordinary work. Approving one would quote a real customer for a party that was called off. Both surfaces now warn loudly when an open draft points at a cancelled plan, and the send confirm repeats it inside the dialog (a banner can be scrolled past; a confirm cannot). Not a hard block — the gate is unchanged. **§18's rule one step further out: the thing that makes a send dangerous has to say so at the moment of sending.** 897 tests, 896 passing.
  - **Phase 4's leftover planner switch, half done and honestly so.** Load-any-plan-by-ref **shipped**: `/api/party-builder/load?ref=` is honoured only for an authenticated admin, and for a portal customer the parameter never reaches the query (not "rejected" — it must not be an enumeration oracle). The planner also now falls back to `bookings.party_type` when `party_tags.location_type` is absent, so an agent-classified `mobile_party` opened from the workspace no longer boots into studio mode and prices the wrong product. **Not done: the real three-way product selector** — the planner has no studio-rental mode at all (no `isRental`, no rental state, only `locationType: host_hampton | mobile`), so that is a feature build inside a 3,748-line component, and it lands on the mobile pricing surfaces Adam is actively reworking. Left for after his numbers land.
- [x] Phase 5 — **the summary page (item 1), live 2026-09-11.** `/plan/[ref]/summary` server-renders the invoice from the DB in the exact section order of `invoices/_template.html` (locked header + services bar, client/event grid, featured + line items with descriptions and Optional tags, Total and Balance Due, the deposit callout OUTSIDE the totals, What's Included studio-only, good-to-know/policies per `party_type`, payment block, mobile menu appendix, locked footer). Gated on the `hh_portal` cookie naming THAT ref; the magic link may redirect here because `/api/portal/auth` matches the path against the ref it just authenticated, so the allowlist stays closed. **migration 037** adds `plan_content` (the invoice's prose per party type, moved out of SKILL.md) with a compiled fallback, `lib/planContent.ts` + `lib/planInvoice.ts` (view model: the studio rule — Balance Due = full Total, deposit separate — and optional items quoted but never totalled) + `lib/invoiceNumber.ts` (issued on FIRST RENDER so a lead that never quotes burns nothing; idempotent on refresh, and two tabs agree on one number). Mobile menu chips are the 26 migration-036 `mobile-station` rows, priceless by construction, minus anything already billed. 26 new tests. **NOT built, deliberately: `PayPanel`, "Email me this", admin "Send to client", `booking_pay_links` persistence and the payment_link webhook match** (items 2-4) — a page that takes money without recording it, or a button that mails a customer without review, is worse than a smaller finished slice. Native admin viewing **shipped later the same day** with migration 038's session cookie — the `hh_admin` cookie is something a server component can read, unlike a Bearer header in `localStorage`, so an admin session opens any plan directly (banner shown, `no-print` so a client's PDF never carries it) and the admin cookie is deliberately NOT scoped to a ref, because an admin is entitled to every plan.
- [x] Phase 5 — **the pay path shipped 2026-09-12 (items 2-4), plan §21.** Built back to front because §17's reason still held — *"the panel is only safe once the webhook records what it charges"* — so persistence and the webhook match landed before any button existed. **`lib/planPayLinks.ts`** derives every figure from `loadPlanInvoice()` (the same call that renders the document) and the client only ever posts a `purpose`: there is no amount in any request body to tamper with, and the one human-supplied figure — an admin `custom` amount — is capped at `remaining + depositOwed`, so an admin who wants to charge more must add the line item to the plan first. The studio rule is **imported, not restated**: `paidTowardTotalCents()` takes `invoice.depositIsSeparate` off the view model, because that flag is the difference between undercharging a studio rental by $250 and double-charging one. **`lib/planPayment.ts`** is the only writer of a plan `booking_payments` row, downstream of the untouched signature check, and it treats **23505 as already-recorded (a success), by CODE not message text** — with nothing downstream re-running on a redelivery, so no second receipt and no rewritten balance. It **credits what Stripe collected, never what the link expected**, which is what makes a link minted before the plan was re-priced safe: the customer paid the old figure and the remaining balance just stays higher. Minting **voids the previous link at Stripe** before creating the new one (a DB-only void leaves a payable URL we have merely stopped tracking) and every link carries `restrictions.completed_sessions.limit = 1`; a row insert that fails **deactivates the link it just made**, because a live link we did not record is precisely the untraceable pay link migration 035 exists to eliminate. `loadPlanInvoice()` and `matchPlanPayLink()` both grew a **third outcome** for the reason the lead-route fix named: a webhook that reads a DB blip as "no such plan" answers 200, Stripe never retries, and a real payment is lost — so a read failure is a 500 and comes back. `lib/planAccess.ts` extracts the summary page's access rule so `/api/plan/[ref]/pay-link` makes the *same* decision (otherwise any portal cookie mints a link against any plan). A payment on a **cancelled** plan is recorded anyway and flagged — the money is real — while *minting* on one is refused and the refusal is written to the ledger. Balance and status are **two UPDATEs**, because `bookings_scheduled_fields_check` refuses `deposit_paid` on an undated plan and bundling them would lose the balance too. Item 4: `/api/plan/[ref]/email-me` (portal cookie, 60s cooldown that declines on a failed read) and `/api/admin/plan/[ref]/send` (admin + explicit `confirm` + `adminActorId`), neither of which will send to an address from the request — only to the one on the plan. **No migration and no new env: 040 stays free for the learning loop.** 80 new tests, **992 total, all green.**
  - **What is NOT proven, and it matters: the container has only a `sk_live` key.** No Stripe test secret exists anywhere in the environment, so an end-to-end payment could not be exercised without charging a real card. The webhook path is covered by 24 unit tests against synthetic sessions (redelivery, forged and missing signatures, a changed total, an underpayment, a cancelled plan, a deleted pay-link row, a legacy pay link falling through, a read failure at each step, the status-constraint refusal) and production was verified for **minting only** — a link minted against a test plan, its `booking_pay_links` row checked against the server-derived figure, then voided. **Needs Adam:** a Stripe test secret + test webhook secret in the container so the recording path can be driven with a test card. **Needs Adam, smaller:** `booking_payments` does not record which payment a `refund` reverses, so refunding a studio *security* deposit reads the balance $250 high — it errs towards asking for more, and it is a hand-entered row either way, so it is stated rather than guessed.
- [x] Phase 5 **review + test — the pay path, EXERCISED (2026-09-11, plan §22, migration 040).** The container still holds only `sk_live`, so rather than charge a card the webhook was driven in production with synthetic `checkout.session.*` events **signed with the real `STRIPE_WEBHOOK_SECRET` from inside the container** — which runs every DB write, status transition and receipt while creating no charge at Stripe at all. **What held:** no amount reachable from any request body (`amountCents`/`amountDollars`/`amount`/`chargeCents` all set to 1 still minted $600), the admin `custom` cap at $850, signature verification failing closed four ways with zero rows written, a byte-identical redelivery AND one with `amount_total` inflated to $9,999 both no-ops, `matchPlanPayLink`'s row-id tier beating metadata that claimed another plan, the studio rule ($250 deposit leaves the $600 balance intact), the two-UPDATE split (balance written, status advance refused on an undated lead), a cancelled plan recorded-and-flagged, and every access door (portal cookie for plan A → 403/404 on plan B; wrong-secret and 8-day-old admin cookies → 403; admin send 400 without `confirm`, 401 unauthenticated; `to` in the body ignored). **Six things were wrong, all fixed and re-verified.** (1) `loadPlanInvoice` discarded the `booking_line_items` read error, so a blip gave `totalCents: 0` and the webhook then marked the plan **`paid_in_full` with a zero balance** — demonstrated live with a $600 payment. (2) **The $927 a real customer paid was recorded nowhere** (see below). (3) §21's "there is never a window with two payable links" was **false**: six concurrent mints produced six live payable links and paying two charged the same $250 deposit **twice**. (4) The unique `stripe_payment_intent_id` constraint three webhook comments rely on **did not exist**, so no `payment_intent.succeeded` path has ever been idempotent. (5) `payment_status: 'unpaid'` was recorded as a full payment, and `amount_total: null` wrote a $0 row that burnt the session id. (6) An **overpayment** was clamped to a $0 balance with no flag anywhere. Plus: the email-me cooldown was check-then-act (five concurrent calls → three emails), the summary page answered 404 for "could not read", the balance button said "Pay $600.00 balance" under an invoice line reading "Balance Due $350.00", and customer-written names went raw into email HTML. **1032 tests (was 992), all green; 0 app-code tsc errors; `next build` clean.**
- [x] **Incident 2026-09-11 — $927 paid and recorded NOWHERE.** Raised by the Phase 5 build session, confirmed independently against live Stripe and the DB, and reproduced in production. A real customer paid $927.00 on a Payment Link **created by hand in the Stripe dashboard**, so its metadata was `{}`; every `m.type` branch declined it, it fell through to the legacy party-booking tail, the `bookings` insert was refused by `bookings_contact_reachable_check` and the handler then **threw** formatting a confirmation email for a customer it did not have — a 500, three days of Stripe retries, and a stack trace in a container log as the only trace. `lib/unclaimedPayment.ts` now records any settled session nothing claims into `financial_transactions` under **"Unmatched Stripe Payment"** (keyed on the session id, so a redelivery cannot double it) and emails Adam once, then acknowledges. **Rule: an unattributable payment is a bookkeeping problem; an invisible one is a lost payment.** The $927 row itself is deliberately NOT backfilled — that is Adam's accounting and it is with him.
- [ ] **Needs Adam — a Stripe TEST key pair in the container.** Only `sk_live` is set (re-confirmed 2026-09-11: `livemode: true` on every Stripe object), so **no real payment has been made and none should be**. Adding `STRIPE_TEST_SECRET_KEY` + `STRIPE_TEST_WEBHOOK_SECRET` (box `.env` + `docker-compose.yml` + `AGENTS.md` §7) would let a test card prove what Stripe itself sends. Until then the signed-synthetic-event harness in plan §22 covers everything except that — which is much more than the 24 unit tests it replaces, and is repeatable.
- [ ] **Needs Adam — the $927 row.** Whether and how to enter it in `financial_transactions` is his accounting call; the code path that lost it is fixed.
- [ ] **Needs Adam — the cron-job.org job for the weekly distill.** `/api/cron/agent-distill?secret=<CRON_SECRET>`, weekly, **Monday 7am** (plan §5). The route is live and was exercised by hand in production; scheduling it needs the cron-job.org account, which the build session does not have. Until it is added the loop captures corrections and can be run on demand, but distils nothing on its own.
- [ ] **Needs Adam — what a `refund` row reverses.** `booking_payments` does not say, so refunding a studio *security* deposit subtracts $250 that was never credited towards the total and the remaining balance reads $250 high. It errs towards asking for more rather than less, and it is a hand-entered admin row either way, so it is documented rather than guessed (plan §21).
- [x] Phase 6 — **the learning loop, deployed 2026-09-12 (plan §23, migration 041).** Allie and Adam have been correcting the agent's drafts since Phase 2 and nothing read those corrections back. **The capture needed no new writer and no new table:** `draft_feedback` is a VIEW over `inquiry_drafts.revisions[]` — for every `sent` draft, the first BODIED revision (not `revisions[0]`, and not the row's own columns, which hold the LATEST text) next to what actually went out, plus the instructions in between. §20's reasoning, unchanged: a second copy agrees with `revisions[]` only while somebody keeps it in step. **`lib/agent/learnings.ts` is the fence**, and it is the point of the phase: `agent_learnings.text` lands in the **TRUSTED** half of the draft prompt while the rows are distilled from draft text the agent wrote in reply to a stranger's email — the same shape as the three prompt-injection doors §13 and §16 found. Four layers, in order of how much each carries: (1) **`is_active` DEFAULTS TO FALSE and the distiller never sets it**, so the customer→prompt chain terminates at a human and a future writer that forgets fails safe; (2) screened on the way in with `containsFabricatedTerms`/`containsForeignContact` — **imported, not restated** (rule 11), which is why those three helpers moved to `lib/agent/draftGuards.ts` and are re-exported from `draftInquiry.ts` so every existing test still reaches the one implementation (rule 7); (3) **screened again on the way out**, because a row being active in Postgres is not evidence it ever passed a screen (rule 8); (4) flattened to one line, since a newline forges a prompt header — §16's exact door. The output-side guardrails are unchanged underneath all of it. **The weekly `/api/cron/agent-distill`** reads a week of feedback, proposes ≤12 `agent_learnings` rows and a voice profile v2, and **activates nothing**; an empty week makes no model call and spends nothing but still writes a ledger line (a run that did nothing must not look like a run that never fired); a failed read of the view is a **503**, not a quiet zero that would show a green cron run every Monday while the loop was dead. The untrusted corpus is fenced by **`JSON.stringify`**, which escapes the newlines a plain-text delimiter could not survive. `/api/admin/learnings` + the Inbox panel let Adam and Allie add a rule by hand, attributed with `adminActorId(req)` throughout; a retired rule is KEPT, because the unique index on its normalised text is what stops the cron re-proposing what a human already rejected. **A loop that changes what the agent writes must not change who approves it:** the route touches neither `inquiry_drafts` nor `bookings`, `approved`/`sent` stay GATED, and a rule proposing otherwise is refused by the screen. **1109 tests (was 1032), all green; 0 app-code tsc errors; `next build` clean.** **Verified in production by CHANGING values, not by reading a page (rule 6):** one authenticated distill run over the real week returned `considered: 3, edited: 1, proposed: 3, refused: [], voiceProfileVersion: 2, costUsd: $0.0248` — and its summary was *correct about the actual correction* (Allie replaced the agent's hedging with a short out-of-area decline). **All three proposals landed `is_active = f` with `activated_by` NULL; voice profile v2 landed inactive with v1 still live.** Then an **ACTIVE hostile row was inserted straight into Postgres**, bypassing the route, the screen and every write guard — and the draft prompt **refused it**, naming *"a prompt section header (\"HARD RULES:\")"*. That is layer 3 proven against the real database. Retiring a rule emptied the prompt; activating a distilled proposal put it in. All five screen refusals fired in production with the reason named, including the documented false positive on *"Never offer a discount"*. Both probe rows deleted afterwards; the three proposals are left inert for Adam and Allie to judge.
  - **The bug found by exercising the view rather than reading it (rule 8).** `was_edited` compared `COALESCE(first,'')` against `COALESCE(final,'')` under a comment claiming this was "NULL-safe, so a draft with no first version reads as not edited". It read as **edited**. Four synthetic drafts with malformed `revisions` — a bare string, an object, an array of junk, `[]` — all came back `was_edited = t` in a rolled-back production transaction. The consequence is worse than a wrong flag: `buildCorpus` would have handed the distiller `agentWrote: ''` beside a full `humanSent`, which reads as *"the agent wrote nothing and the human wrote all of this"* — **a correction that never happened, in the one input whose entire output is rules derived from corrections. Fabricated training signal is worse than none, because it becomes a standing rule.** Fixed in both places (`has_first_version` on the view; `buildCorpus` keeps such a row only when a human really left an instruction) and re-verified: `f, f, t, f`. **Migration 041 applied and re-applied clean, with `pg_indexes` read directly rather than trusted (rule 13).** No sequence was touched: `last_value` is still 118 and the next invoice number is still `444124-000119`.
  - **The second bug the tests could not have caught.** The first authenticated cron run answered **400 on every call** — `For 'array' type, property 'maxItems' is not supported`, then after that fix `For 'number' type, properties maximum, minimum are not supported`. A mocked `fetch` cannot see either: the schema type-checked, 1106 tests passed, and the weekly run would have 502'd every Monday for as long as nobody looked. Rather than guess a third time one redeploy apiece, the **live API was probed directly**: `enum`, `required`, `additionalProperties` and `maxLength` are all accepted; only those two are not, and that is now written down beside the schema. Nothing was lost — both limits were load-bearing in CODE anyway (`slice(0, MAX_PROPOSALS)`, `sanitizeVoiceProfile`, and `proposeLearning`'s clamp of `confidence`). The tripwire test bans both keywords **and asserts `maxLength` is still present**, so the fix cannot later be mistaken for "strip every constraint". **Rule 8 in its plainest form: a schema that type-checks is not a schema the API accepts.**

- [x] Phase 6 review + test — **done 2026-09-12 (plan §24, migration 042).** Link 4 attacked §23's four layers rather than summarising them. **Layer 1 held everywhere** — an INACTIVE hostile row inserted straight into the live table was invisible to the exact query the draft node issues (`rows_the_draft_node_would_see = 0`). **Layers 2, 3 and 4 did not, and the field §23 never screened at all is the one that had already cost a draft.** (1) **`sanitizeVoiceProfile` ran only on what the distiller PROPOSED; the LIVE profile had never been near it.** v1 — hand-written in Phase 1 — put seven exemplars into the TRUSTED half of the prompt, three of them quoting mobile prices ($1,100 / $950 / $990 / $300 / $40 / $35), alongside "real numbers inline with 'starting at'" — into the same prompt that says *"ABSOLUTELY NO PRICING. Not one number with a currency attached."* **`HH-2026-4295` is what that looks like from outside:** parked with *"model included a dollar amount on an info-gather draft twice"* — it broke the rule, was corrected, and broke it again, because the trusted half of its own prompt was showing it how. The output guardrails held and **no wrong price reached a customer**; the cost was a parked reply and a wasted retry. `loadVoiceProfile` now screens on the way OUT — the argument §23 already made for `loadActiveLearnings`, applied to the one table it was not applied to — and reports the drops to the ledger, the log and the panel. Verified after deploy: the three priced exemplars named and dropped, the block now matches no `$`, four exemplars surviving. (2) **Layer 4, "flattened to one line", did not hold:** `flattenToOneLine` mapped only cp < 0x20 and 0x7f, so **U+2028, U+2029, U+0085 and the C1 block passed through — and `JSON.stringify` does not escape them either**, so the distiller's corpus fence passed them too. A learned rule could open a second line in the trusted section and forge a header while rendering as ONE line in the panel, to the very person whose approval is layer 1. Bidi and zero-width controls are now stripped for the sharper version of the same reason. (3) **The screen's header list was written from memory** — `VOICE:`, `PARTY TYPE CONTEXT:`, `CORRECTION —`, `Classifier confidence:` and `Booking reference:` are headers the prompt really uses and the screen had never heard of; added, plus a generic ALL-CAPS-phrase-then-colon rule. (4) **`draft_feedback` guessed at a revision body that was not a string** — the fifth malformed shape, and link 3's bug in the form link 3 left behind: `->>` serialises an object rather than returning NULL, so `[{"email_draft":{"a":1}}]` read `has_first_version = t, was_edited = t` and would have taught the distiller a correction in which the assistant wrote a JSON fragment. **Rule 15: an input the pipeline cannot interpret must be DROPPED, never guessed at.** Migration 042 requires `jsonb_typeof = 'string'` for bodies and notes, and caps the body columns at 8000 chars — one probe row returned `final_email` at 2,000,000 and `reviewer_notes` at 688,894, and the cron reads forty. The flags stay computed on the full text. (5) **Two comments asserting the opposite of their code (rule 8, again):** `nextVoiceVersion` said "the caller retries" and no caller did, so a version collision silently lost a week's voice profile — now retried, bounded, by code 23505; and `activateVoiceProfile` reported "No voice profile is active" unconditionally, which is false in the one race that reaches that branch. **The gap §23 could not close is now closed:** the distiller was driven by an ACTUALLY hostile corpus against the real API — a JSON-string escape, a U+2028 forged `SYSTEM:` header, a plain-language instruction to the reviewing model, a bidi override and a padding attack — and **proposed zero learnings**, naming every attack in its summary including the Unicode trick; no payload survived into the output. **1154 tests (was 1109), all green; 0 app-code tsc errors; `next build` clean.** 042 applied and re-applied clean; the three real sent drafts read identically after it; sequence still 118/t. **Migration 042 is taken — the next free number is 043.**
- [x] **Agent content pipeline — built, then reviewed. Live 2026-09-12** (`docs/content-pipeline.md`). Link 6 built COPY → `website_content` → cache → published page and drove it end to end in production; link 7 attacked it and found **four** more defects, three of them inside the two hand-written screens link 6 had added — which is where §2 predicted the next problem would be. (1) **One backslash defeated the protocol-relative guard.** `safeImageUrl` reasoned `startsWith('//')` is another origin and `startsWith('/')` is ours; the WHATWG URL spec normalises a backslash to a slash in a special scheme, so `/\evil.example.com/x.png` is **site-relative to the screen and `https://evil.example.com/x.png` to every browser** — and it landed in `<img src>` on the public page and in `og:image`, the same two readers link 6's own probe had just had to fix for that same field. The screen now **parses** with `new URL` and checks the host the parser agreed on, returning the re-serialised URL rather than the string it was handed. (2) **It accepted any host on the internet.** The `<img>` leaks every visitor's IP to that host; the sharper half is `og:image`, where the card Facebook and iMessage render for a Host Hampton URL becomes whatever that host serves **and can change after a human approved the row**. Now an allowlist of `SITE_URL` + the Supabase bucket, with a test that reads `next.config.js` off disk so the two cannot drift (rule 11). (3) **`containsMarkup` and `htmlToPlainText` carried two different definitions of "a tag" in one file** — the predicate said `<` + `[a-zA-Z!/?]`, the reducer deleted `<[^>]*>` — so `"Groups of <10 guests and >4 adults"` came out `"Groups of 4 adults"` and `"5 < 10 and 20 > 3"` came out `"5 3"`. The renderer calls the reducer **unguarded** on every FAQ question, FAQ answer and section heading, and `buildJsonLd` calls it on what goes to Google, so it ran on every render of every published page; the file's own tests already asserted those strings were not markup, and nothing had put the two functions in one test. One shared source now, asserted in both directions. **Measured: no live row contains a `<` at all, so nothing published was corrupted.** (4) **The SEO budget trim split emoji in half** — `slice()` counts UTF-16 code units, so a description ending on an emoji lost half a surrogate pair and would have published **U+FFFD in the search result**. Plus a note that claimed "markup removed" when nothing was removed (rule 10 in reverse). **Held under attack and reported as such:** `carriesPublishedPrice` traverses fully with a `WeakSet` (no depth-cap bypass), the JSON-LD `<`-escape already closed the `</script>` breakout, the publish gate really is the only door to `published` (every `advance()` call enumerated), nothing writes a published row without flushing, the preview route has no second reader, and the route walker handles route groups. **Rule 12 exercised rather than trusted:** the production build against a broken `SUPABASE_URL` returns **500, not 404**, on published pages — and, newly measured, on *every* unmatched URL, so a database outage presents as a site-wide 500 rather than a content-only one. **Known gap, stated not closed:** nothing re-checks a published row if a static page shadows it later; the sitemap skips-and-logs it and the panel badges it, so it goes quietly dead rather than lying. **No migration — 043 is still free.** 1453/1453 green.

## Phase 4: Campaign Automation

**Measured starting state, 2026-09-12 (link 7) — check it again before trusting
it.** Counted from the nginx container's own log, filtered to Host Hampton's
route names **and by status code**, because that nginx fronts ~30 businesses on
this box and an unfiltered `grep` counts other people's crons (an unfiltered
count also returns `444`s, which never reached an app at all):

| cron route | hits | verdict |
|---|---|---|
| `agent-dispatch` | **996 × 200** | the only Host Hampton cron genuinely scheduled and working |
| `agent-distill` | 1 × 200, 4 × 401, 4 × 502 | hand runs + the schema 502s §23 documented; not scheduled |
| `draft-newsletter` | **11 × 401, 0 × 200** | something calls it and it has **never once authenticated** — a job that looks scheduled and has never done anything |
| `send-campaigns` | **0** | never called |
| `process-sequences` | **0** | never called — **and this route already exists**, so Phase 4's "email campaign sequencing" is partly built, not greenfield |
| `send-reminders` / `event-reminders` / `birthday-rebooking` | **0** | never called — and link 10 later showed the sharper fact: `birthday-rebooking` HAS run once (2026-08-17, outside the window, per `marketing_ledger`), and the other two could not have worked if they had been called. See `docs/reminder-engine-review.md` §1. |

So **Phase 3B's whole reminder-and-campaign engine has never run in
production.** It is code with a test suite and no operational history, which is
a different risk profile from code that works — and the first Phase 4 job is to
find out which parts of it are real. Note also that the Brevo list is **944 real
people** ([[comms-provider-config]] / `PLAN.md` Phase 3B): nothing here may
auto-send, and a campaign is never a test.

**Both halves of that paragraph turned out to be wrong, in opposite
directions.** Link 8 found the *campaign* half had run for five months
(below). Link 10 found the *reminder* half was worse than "never run": it
**could not have worked if it had run**, because `scheduled_reminders` rejected
every insert the code made and no writer read the error, so the queue held zero
rows in five months (`docs/reminder-engine-review.md`, migration 044). "No
operational history" and "no operational *possibility*" look identical from
outside and are not the same thing — which is the sharpest version yet of rule
17: **ask the table that would hold the evidence, and then check that the table
would have ACCEPTED the evidence.**

**The table above is a TEN-DAY WINDOW, and it is wrong about history** (link 8,
2026-09-12 — `docs/phase-4-campaign-automation.md` §1). `docker logs
hampton_nginx` starts at `02/Sep/2026:00:53:15`. The database disagrees with it:

- **`process-sequences` ran every fifteen minutes for five months.** 57
  enrollments carry a `last_sent_at` between **2026-03-21** and **2026-08-16**, on
  15-minute boundaries. It sent real marketing email to real customers and then
  its cron-job.org job disappeared. **44 enrollments are still `active`, frozen
  mid-sequence.**
- **`draft-newsletter` ran daily at 11:00 UTC** until 2026-07-17 and left **17
  `event_update` drafts** in `scheduled_campaigns`, seven of them for the same
  event, none ever sent. It has 401'd once a day since ~2026-07-18, as has
  `booking-locks` at 04:00. **The container's `CRON_SECRET` is correct** (a
  request made from inside the container 200s on both), so those two
  cron-job.org jobs hold a stale one — **needs Adam.**

The lesson: **a log is a window, and a count over a window is not a statement
about history.** "Never run" and "ran for five months and stopped" are different
risk profiles — the second means anything wrong with the code has already
happened to real people, which is exactly what §2.1 below turned out to be.

- [x] **Email campaign sequencing (LIST + OUTBOUND)** — **done 2026-09-12, migration 043, `docs/phase-4-campaign-automation.md`.** Not greenfield: `/api/cron/process-sequences` had already sent 57 real emails, with **four defects in code that had already run.** (1) **Its activity log never wrote a single row.** It inserted `type: 'sequence_email_sent'`, which violates `contact_interactions_type_check`, on a `.then(() => {})` that discarded the error — confirmed in production with a rolled-back INSERT. 57 marketing emails to real customers, zero records, for five months. `lib/contactInteractions.ts` now holds the CHECK's contents read out of `pg_constraint`, types the writer against them, and **reports** a failure. (2) **Check-then-act on a path that emails customers**: read `current_step`, send, write it back — two ticks both send. `email_sequence_sends` + `UNIQUE (enrollment_id, step_number)` is now CLAIMED BEFORE THE SEND, 23505 handled by CODE; the send row is marked `sent` *before* the enrollment advances, so a crash between them is recovered by catching up rather than re-sending; a stale claim is taken over by a CONDITIONAL update; an **unreadable** claim timestamp counts as fresh, never abandoned. (3) **A transient failure was terminal twice, in opposite directions** (rule 12, twice in nine lines): an unreadable STEP marked the enrollment `completed`, permanently ending a customer's sequence over one bad second; an unreadable CONTACT marked it `unsubscribed` — a claim about what a person asked for, made on a failed SELECT. Every lookup is now `found | absent | unavailable`. A third instance was in `isDue`: an unparseable `event_date` gave an Invalid Date, every comparison false **including `now < dueDate`**, so a malformed date sent every remaining step at once. (4) **The mail had no unsubscribe link at all** — no link, no header, no route, which is also why the opt-out state it checks could only be updated from somewhere else. `lib/unsubscribeLink.ts` (HMAC on the existing `PORTAL_LINK_SIGNING_SECRET`, **no expiry**, no new env), a `/unsubscribe` page that **never acts on GET** because mail scanners fetch every link, and `POST /api/unsubscribe` doubling as the RFC 8058 one-click endpoint. No address is read from the request — only the one the token names. Also: customer-written `first_name` went RAW into HTML (rule 5, same defect as plan §22's receipts) and unflattened into the SUBJECT, which is a mail header. **The opt-out check was already in the right place** — at send time, not enrolment — and it stays there, now reading both `email_opt_in` and `contacts.status`.
- [x] **Social content calendar auto-generation (COPY + SOC)** — **done 2026-09-12.** A week of Instagram drafts written by Claude into `social_posts` at the column default `draft`, reviewed in a new **Social** admin tab. **`social_posts` ALREADY EXISTED** from the orchestrator schema — empty, no writers, different shape — and the first `CREATE TABLE IF NOT EXISTS` was answered `relation "social_posts" already exists, skipping`, which would have left the app inserting columns that do not exist against enums it did not know. 043 **extends** it instead, and the code uses the LIVE enum labels read out of `pg_enum`: `facebook_page` (not `facebook`), no `pending_review`, `created_by` is the `agent_name` enum so it is always `SOC` with the human requester in `generation_meta`, and `scheduled_for` is a timestamptz so every slot is noon UTC and the unique index is over the UTC calendar day. Four guardrails, §23/§24's shape: `status` DEFAULTS to the safe value; the prompt's untrusted half is fenced with `JSON.stringify`; screened on the way IN with `containsFabricatedTerms` called with an **EMPTY allowed-amount set** — a caption may state no dollar figure at all, not even the $250 deposit a draft may name — plus `containsForeignContact`, `containsMarkup` and `safeSiteLink`; and **screened again on the way OUT**, because a row being in Postgres is not evidence it passed a screen. `approved`/`published` are GATED edges. **Nothing posts anywhere**: "Published" means Allie recorded that she put it up. **Driven in production** — 3 real drafts for 3 real events at $0.0054, a re-run that inserted nothing and spent nothing and SAID so, then attacked with four hostile rows inserted straight into Postgres (a price, a foreign link + `/\evil.example.com/x`, a U+2028-forged `SYSTEM:` header, a `<script>`): all four refused with the reason named in the panel, the ledger and the log, all four approve attempts 422 — **and a legitimate draft still approved cleanly**, both directions.
- [x] **The campaign sender could not tell "delivered" from "created".** `sendCampaign` returned the campaign id after a FAILED `/sendNow` and both callers wrote `status: 'sent'` — a send to **944 real people** reported as delivered when it was only created, and not retryable either because a retry makes a SECOND campaign at Brevo. Three outcomes now, with the Brevo id kept on the failure so the orphan is traceable. Both send paths were **check-then-act on those same 944 people** (two clicks on Send both saw `draft`); both now claim with one conditional UPDATE and a second attempt is a 409. **A regression this introduced and then fixed:** claiming strands the row at `sending` on any path that does not attempt a send, so those paths release it explicitly. `draft-newsletter` now refuses to draft while an unread `event_update` draft is waiting (and names it), reads the first TEXT block (rule 1), and **screens the model's subject and intro**, which went unscreened into a body bound for 944 inboxes.
- [ ] **Needs Adam — Automated Instagram posting via Meta API (OUTBOUND).** Measured 2026-09-12: the container has **zero** `META_*` environment variables, so this is blocked on credentials before it is blocked on code.
- [ ] **Needs Adam — Google Business Profile post automation.** Same: no credential in the container.
- [ ] **Needs Adam — whether to reschedule `process-sequences`.** The code is ready and safe; the DATA is not. Turning the cron back on immediately processes **44 enrollments frozen since 2026-08-16**, whose next steps are weeks or months overdue — *"Still thinking about your event?"* to people who enquired in July about an event that has already happened. That is a business call and it mails real customers, which is the one case the chain's judgement rule stops for. **Three options now, because link 9 added the third:** reschedule as-is; cancel the stale enrollments first; or **drain it with `?limit=1`** — `/api/cron/process-sequences?secret=<CRON_SECRET>&limit=1` processes only the single longest-waiting enrollment per tick (the scan is `ORDER BY enrolled_at ASC`, so the cap is deterministic), which means one real person at a time with a look in between. **Read the note under §11.1 first:** seven of the 44 have a mixed-case email address and their unsubscribe link did nothing at all until 2026-09-12, so they are the people most likely to press Spam rather than Unsubscribe.
- [x] **Phase 4 review + test — done 2026-09-12 (link 9, `docs/phase-4-campaign-automation.md` §11). No migration — 044 is still free.** **Seven defects, and the first had already happened to real people.** (1) **The unsubscribe link did nothing for 21 real contacts, seven of them mid-sequence.** `generateUnsubscribeToken` lowercases the address deliberately; `contacts.email` is plain `text` with a unique index on the RAW value and **21 of 1216 rows are not lowercase**; `.eq('email', …)` is case-sensitive. Those people clicked Unsubscribe, got 200 and a confirmation page, and **not one byte was written** — CAN-SPAM before it is engineering, and rule 10's other half in the most expensive place there is. **The Brevo webhook had the same bug in six places**, so every `unsubscribed` and `hardBounce` it ever reported for those addresses updated zero rows — and that column, `email_opt_in`, is the first thing `optedOutReason()` reads before every marketing send. One shared `findContactsByEmail()`; `ilike` fetches candidates and is NOT trusted to be the answer (`_` is a LIKE wildcard and would sweep up a stranger), every write is by `id`, and a failed read is a 500 so Brevo retries. (2) **`{ok:true, alreadyOff:true}`** for an unknown address — the exact oracle the comment three lines above it forbade. (3) **`safeImageUrl` still returned another origin**: `https://www.hosthampton.com//evil.example.com/x.png` has OUR host, so the allowlist passed, and `url.pathname` is `//evil.example.com/x.png` — protocol-relative in every `<img src>` and every `og:image`. Content-pipeline §11.1 through a second door. (`safeSiteLink` was sound.) (4) **`screenStoredPost` never read `hashtags` or `image_idea`**, both of which the Social tab renders next to the caption and Allie acts on — and the PATCH edit path stored hashtags under a weaker rule than the generator's own parser. Rule 11's sharpest form, twice. (5) **`sanitizeCaptionText` missed the Unicode TAG block** (U+E0000–E007F — invisible ASCII, the current fashionable injection), U+180E, LRM/RLM/ALM, the invisible math operators, the Hangul fillers and **lone surrogates**. (6) **A FOURTH writer of opt-out state, writing the wrong way:** `upsertContact()` put `status: 'lead'` in an upsert payload, so an admin-set `unsubscribed` was erased by that person's next enquiry form — and 429 `customer` rows were demoted the same way. (7) **The SMS campaign branch recorded `sent` with `total_recipients` = attempted** even when every message failed — §3's defect, one branch over. **Driven in production:** the sequencer's send path run end to end for the first time (one real Resend send to a throwaway probe, `contact_interactions` row written — §2.1 proven on the send path itself), **five concurrent ticks producing exactly one winner** against the real unique index, opt-out flipped mid-sequence and honoured on BOTH halves, the unsubscribe endpoint attacked nine ways, and four new hostile social rows (price in `image_idea`, foreign handle in a hashtag, TAG characters in the caption, foreign link in `image_idea`) all refused and 422'd **while a legitimate draft still approved**. Everything restored: 44 active / 48 completed / 27 unsubscribed, three real social drafts, sequence still 118/t. **1573/1573 green** (was 1542).

## Phase 5: Memory + Learning

- [ ] Memory update pipeline: agents write back learnings post-task
- [ ] A/B content testing (COPY generates variants, INTEL tracks performance)

## Future: Multi-Business Scaling

- Branch-per-business model (not SaaS multi-tenant)
- Same codebase, different .env + Supabase + agent_memory seed data
- Target businesses: MyGravelGuy.com, Eastern Landscape (easternlm.com)

## The autonomous build chain (Adam, 2026-09-12)

Adam asked for a self-driving sequence rather than one session at a time:
**Phase 5 build → Phase 5 review/test → Phase 6 build → Phase 6 review/test →
continue until the project is complete.** Each session spawns its own successor
as its last act, so the chain does not depend on any one session staying alive.

| # | Session | Status |
|---|---|---|
| 1 | Phase 5 — the pay path (items 2-4) | **done 2026-09-12**, worktree `sandy-lotus`, plan §21. No migration — **040 still free**. 992/992 green. Production verified for MINTING only; a real payment is unproven because the container has only a live Stripe key. |
| 2 | Phase 5 review + test | **done 2026-09-11**, worktree `pure-otter`, plan §22. Drove the webhook in production with SIGNED synthetic Stripe events — no card charged, because only `sk_live` exists. Found six real bugs including a lost $927 customer payment, a failed read that marked plans `paid_in_full`, and six simultaneously payable links for one deposit. **Took migration 040.** 1032/1032 green. |
| 3 | Phase 6 — learning loop (**migration 041**) | **done 2026-09-12**, worktree `endless-badger`, plan §23. `draft_feedback` view + `agent_learnings`, the weekly distiller, the draft prompt, the Inbox panel. **Took migration 041 — the next free number is 042.** Found and fixed **two** real bugs, both by exercising rather than reading: `was_edited` read a malformed `revisions[]` as a rewrite (fabricated training signal), and the Anthropic schema carried two keywords the API refuses, which 400'd every distill call and no mocked test could see. 1109/1109 green. |
| 4 | Phase 6 review + test | **done 2026-09-12**, worktree `quick-wolf`, plan §24. Attacked §23's four layers. Found **five** real defects: the LIVE voice profile was never screened and was feeding the trusted prompt mobile prices while the same prompt said "ABSOLUTELY NO PRICING" (`HH-2026-4295` parked for exactly that); `flattenToOneLine` let U+2028/U+2029/NEL/C1 through, and `JSON.stringify` does not escape them either; the screen's header list missed five headers the prompt really uses; `draft_feedback` read a serialised JSON object as a draft body; and two comments asserted the opposite of their code. **Took migration 042 — the next free number is 043.** Drove the distiller with a genuinely hostile corpus against the real API for the first time: zero learnings proposed, every attack named. 1154/1154 green. |
| 5 | SEO optimization pass | **done 2026-09-12**, worktree `grand-cardinal`, `docs/seo-pass.md`. Measured the live site instead of trusting the brief: nine of the twenty "pages with no metadata" inherit it from a sibling layout and two are bare redirects. Found that **`/book` served ten characters of HTML** (a `useSearchParams()` client page bails to its Suspense fallback during a static prerender, and the AI crawlers robots.ts invites by name run no JS), that **every `Event` rich result on the site was invalid ISO 8601**, that **40 of 69 URLs had no `og:image`** because Next merges metadata shallowly, and two structured-data prices that disagreed with their own pages. **No migration — 043 is still free.** 1284/1284 green. |
| 6 | agent content pipeline: COPY → `website_content` → ISR | **done 2026-09-12**, worktree `windy-summit`, `docs/content-pipeline.md`. Measured first: the pipeline had never produced a row (all five rows `created_by='manual'`) and the weekly cron is **not scheduled** (0 nginx hits in 10 days). Found that **all four English drafts are shadowed by hand-built static pages** and would publish to a URL that renders somebody else's page; that the renderer handed **agent-written HTML to `dangerouslySetInnerHTML`, including in the admin preview modal**; and that route-level `revalidate` **does not work on any Supabase-reading route** (`cache: 'no-store'` opts it out of static rendering) — measured, not assumed. Drove COPY → published → live page → sitemap end to end, proved the cache by CHANGING a value both ways, and attacked it with a hostile row that found **two unscreened readers it had just written**. **No migration — 043 is still free.** 1437/1437 green. |
| 7 | agent content pipeline — review + test | **done 2026-09-12**, worktree `velvet-moss`, `docs/content-pipeline.md` §11. Attacked §2's two hand-written screens and found **four** real defects, three of them in those screens: **one backslash defeated the protocol-relative guard** (`/\evil.com/x.png` is site-relative to the screen and another ORIGIN to every browser — into `<img src>` and `og:image`, the same two readers link 6 had just fixed for that field); `safeImageUrl` accepted **any host on the internet**, which makes our share card attacker-controlled and changeable after approval; `containsMarkup` and `htmlToPlainText` carried **two different definitions of a tag** in one file, so `"Groups of <10 guests and >4 adults"` rendered as `"Groups of 4 adults"` on every page view; and the SEO budget trim **split emoji in half**, putting U+FFFD in a meta description. Rule 12 exercised rather than trusted (a broken DB really does return 500, not 404 — and, newly measured, so does every unmatched URL). **No migration — 043 is still free.** 1453/1453 green. A fifth defect was found by the production probe itself: all three hostile image URLs were correctly refused and the log said **nothing** — a screen that worked perfectly and told nobody (rule 10). |
| 8 | Phase 4 — campaign automation (the buildable half) | **done 2026-09-12**, worktree `proud-badger`, `docs/phase-4-campaign-automation.md`. **Took migration 043 — the next free number is 044.** Measured first and the brief's own table did not survive it: the nginx log is a **ten-day window**, and the database proves `process-sequences` **ran every fifteen minutes for five months** and stopped on 2026-08-16 with 44 enrollments frozen. Found **four defects in code that had already sent 57 real marketing emails** — an activity log that never wrote a row because its `type` violates a CHECK and the error was discarded; check-then-act on the send; a transient read failure marking a customer `completed` *and* another marking one `unsubscribed`; and no unsubscribe link of any kind. Plus a campaign sender that reported a created-but-undelivered Brevo campaign as `sent` to 944 people, and two check-then-act send paths. Built the social content calendar and attacked it in production with four hostile rows — all refused, a legitimate draft still approved. **1542/1542 green.** Did NOT reschedule the sequencer: that mails 44 real people and is Adam's call. |
| 9 | Phase 4 — campaign automation, review + test | **done 2026-09-12**, worktree `amber-mountain`, `docs/phase-4-campaign-automation.md` §11. **No migration — 044 is still free.** Found **seven** defects. The one that matters: **the unsubscribe link built by link 8 did nothing at all for 21 real contacts, seven of them on an active enrollment** — the token lowercases the address deliberately, `contacts.email` is case-sensitive `text`, and 21 of 1216 rows carry capitals, so those people got a 200 and a confirmation page over a write that never happened. **The Brevo webhook had the same bug**, so their unsubscribes and hard bounces never reached `email_opt_in` either — the column the sequencer reads before every send. Also: the unknown-address branch was the oracle its own comment forbade; `safeImageUrl` still returned another origin through `https://www.hosthampton.com//evil.example.com/x.png`; the output screen had never read `hashtags` or `image_idea`, both of which the panel renders; `sanitizeCaptionText` missed the Unicode TAG block and lone surrogates; `upsertContact` was a fourth writer of opt-out state resetting `status` to `lead`; and the SMS campaign branch still could not tell delivered from attempted. **Closed §6's stated gap:** the sequencer's send path was driven end to end in production behind a new `?limit=N` cap, and five concurrent ticks produced exactly one email. **1573/1573 green.** |
| 10 | Phase 3B — the reminder engine, review + test | **done 2026-09-12**, worktree `sharp-falls`, `docs/reminder-engine-review.md`. **Took migration 044 — 045 is next.** Measured first, and the table disagreed with everything: **`scheduled_reminders` held ZERO rows, ever.** The queue had been rejecting every insert for five months and no writer read the error — `reference_id` was `uuid` while four of the five enqueue functions write a booking_ref (**22P02**), and the `reference_type` CHECK allowed `('event_ticket','booking')` while the code writes `'event'` (**23514**). `lib/checkinReminders.ts` carried a comment asserting the column was keyed by booking_ref (rule 13), and `checkin_tokens` is empty because of it. So no booking reminder, balance chase, thank-you, review request or pre-arrival check-in text has ever been sent on a schedule. Also: the sender had **no claim** (read→send→mark, so two ticks both sent) and wrote `status='sent'` over six different non-sends including an unset `RESEND_API_KEY` and a provider rejection (rule 10's expensive half); **email consent was never re-read at send time**, so the birthday marketing nudge went out a day after its only opt-in check; `/api/cron/event-reminders` **never consulted `contacts` at all** — no `sms_opt_in`, no STOP, no cross-run idempotency — and would have double-texted once the queue worked; `.eq('email',…)` in seven places (the §11.1 defect again, so the 21 mixed-case contacts got no reminders at all); eight lookups collapsed "could not read" into "not found"; birthday-rebooking nudged **cancelled** parties; the SMS budget was charged 1 for a 3-segment emoji template; and one em dash made every check-in text 4 segments instead of 2. Proven in production: the engine **delivered its first email ever** (real Resend id), five concurrent ticks → exactly one send, every guardrail refused AND named its reason, a transactional email still reached an opted-out contact while the marketing one was refused, and `event-reminders` run four times produced **one** row. Tests now run against a fake store that models column types, CHECKs, the unique index and case-sensitive `.eq()` — the old mock accepted anything, which is why a green suite agreed with code Postgres had been refusing for five months. **1573 → 1646 green.** |
| 11+ | remaining work, assessed by the chain | — |

**If the chain stops**, it is because a link failed before spawning its
successor. Restart it by spawning the next link by hand with the same brief
shape — the briefs are self-contained by design and each one carries the
standing rules forward verbatim.

**The judgement rule every link carries:** decide technical questions and keep
moving; if something needs Adam (a price, a business rule, a missing
credential), record it as "needs Adam" in PLAN.md and the memory, skip that
item, and carry on rather than blocking the loop. The single exception is
anything that could charge a real customer incorrectly — that stops and asks.

**Known blocked, do not start:** the planner's three-way product selector, which
needs the mobile pricing numbers only Adam has (§15).

**Needs Adam, carried forward (do not guess, do not block on):**
1. **The cron-job.org job** for `/api/cron/agent-distill?secret=<CRON_SECRET>`,
   weekly Monday 7am (§23, §24). The route is live and runs correctly by hand;
   scheduling it needs the cron-job.org account. Until then the loop captures
   corrections and distils nothing on its own.
2. **Voice profile v1 tells the agent to quote prices** in prose the screen
   cannot see — "real numbers inline with 'starting at'", "State a real starting
   number fast", "Quote a real starting price" (§24). The figures themselves are
   now stripped, and the info-gather block states that it overrides OPERATOR
   VOICE; the clean fix is a rewritten **v3 profile**, and Allie's voice is
   Allie's to write.
3. **The three distilled proposals** sitting inactive in `agent_learnings`.
   Whether they become standing rules is a voice decision.
4. **`/first-birthday-parties` contradicts itself on price** (SEO pass,
   `docs/seo-pass.md` §11). The prose reads *"packages start at $850 for up to
   10 guests"*; the pricing table thirty lines below reads **$650** (up to 8),
   **$800** (up to 10), **$1,045** (up to 12). Both are visible on the same live
   page. The structured data now publishes the TABLE ($650–$1,045), because that
   is what a visitor can see and Google requires the markup to match — but one
   of the two numbers has to go, and which one is Adam's call.
5. **Cloudflare's Managed robots.txt is blocking every AI crawler the site is
   trying to attract** (`docs/seo-pass.md` §8b). The file served at
   `www.hosthampton.com/robots.txt` is NOT what `src/app/robots.ts` emits:
   Cloudflare prepends a "BEGIN Cloudflare Managed content" block carrying
   `Content-Signal: ai-train=no` and `Disallow: /` for **ClaudeBot, GPTBot,
   CCBot, Google-Extended, Bytespider, Amazonbot, Applebot-Extended and
   meta-externalagent** — the exact crawlers `robots.ts` names in order to
   WELCOME them. Our rules still follow it, so the served file holds two
   contradictory groups per user-agent. This is the same defect as `/book`, one
   layer out: the site is trying to be cited by AI answer engines and the edge
   is telling them no. Fixing it is a zone setting in the Cloudflare dashboard,
   which no build session has a login for.
6. **The cron-job.org job for the weekly town drafts** —
   `/api/cron/weekly-town-drafts?secret=<CRON_SECRET>`, weekly
   (`docs/content-pipeline.md` §8). Measured: it appears **0 times in 10 days**
   of nginx logs, so it has never run and the pipeline has never produced a row
   on its own. The route is live and was exercised by hand in production in
   both auth forms; it is self-throttling (2 towns per run, a no-op once all 11
   have a draft) and lands everything at `pending_review`, so it is safe to
   leave scheduled indefinitely. **Second job in the same backlog as item 1.**
7. **Four town drafts are waiting for review** in the Marketing tab — East
   Hampton, Smithtown, Patchogue, Islip. Model-written copy nobody has read.
   Nothing publishes without a click.
8. **`permanent-jewelry-southampton` is LIVE** — the first agent-written page
   this business has published, put up deliberately as the end-to-end proof.
   Accurate, priceless, in budget, about a real service. **Archive** in the
   Marketing tab removes it in one click if Adam or Allie would rather it were
   not there.
9. **The four dead English drafts** (`first-birthday-parties`,
   `communion-party`, `fundraiser`, `cm-cheer`) are shadowed by hand-built pages
   and hold no content. They cannot publish, but they sit in the review queue
   forever. Archiving them is a content call.
10. **No Google Search Console access.** Which URLs currently rank could not be
   checked, so every canonical / `robots` / redirect decision in the SEO pass
   was made on the conservative side (`noindex` only where a page is
   structurally not content; 308 only on two unambiguous consolidations, with
   `/cm-cheer` deliberately left at 307 as the reversible state). A Search
   Console login would let a later pass verify indexed coverage and confirm the
   Event rich results start validating now that `startDate` is real ISO 8601.
11. **The Twilio number's inbound webhook points at another system** (Phase 3B,
   measured 2026-09-12). +1 844 624 0400 routes inbound SMS to a **different
   Supabase project's** edge function, so `/api/webhooks/twilio` has never
   received a message and a customer's **STOP never reaches our `contacts`
   table**. Twilio honours STOP at the carrier level, so nobody is messaged
   against their wishes — but the DB keeps them marked opted-in and the reminder
   engine keeps trying. The agent's booking conversations are **not** affected
   (they go through Quo, on a different number, with a working webhook). Not
   repointed by a build session because it is a live number feeding something
   else, which is Adam's call; when he confirms, it is one API call.
12. **Two cron-job.org jobs hold a stale `CRON_SECRET`** (link 8, measured
   2026-09-12). `draft-newsletter` (daily 11:00 UTC) and `booking-locks` (daily
   04:00 UTC) have 401'd every single day since ~2026-07-18. The container's
   secret is **correct** — a request made from inside the container returns 200
   on both routes — so it is the jobs that are stale, and `agent-dispatch`
   (998 × 200) holds the current value. One edit each at cron-job.org. Third and
   fourth items in the same backlog as items 1 and 6.
13. **`/api/cron/social-calendar` needs a weekly cron job** —
   `?secret=<CRON_SECRET>`, weekly, Monday ~8am. Self-throttling (it asks which
   slots are already drafted BEFORE calling the model, so a duplicate delivery
   costs nothing) and everything lands at `draft`, so it is safe to leave
   scheduled indefinitely. **Fifth job in the same backlog.**
14. **Three social drafts are waiting for review** in the new Social tab — the
   first copy the SOC agent has ever written. Accurate, priceless, about real
   upcoming events. **The model misspelled the brand hashtag as `#hosthamption`
   in all three**, which is worth a look before any of them goes up.
15. **17 stale `event_update` newsletter drafts** from 2026-07-01 → 07-17 sit in
   the Campaigns tab, seven of them for the same Bitchy Bingo night with seven
   different subject lines. Nothing will add to the pile now (the route refuses
   to draft while one is waiting). Clearing them is a content call.
16. **Two campaigns are recorded as `sent` with `total_recipients: 0`** — Brevo
   ids 1 and 2, on 2026-04-23 and 2026-07-06. Given that `sendCampaign` used to
   report a created-but-undelivered campaign as `sent`, at least one of those may
   never have reached anybody. Only the Brevo dashboard can answer it. (Link 9
   found a second reason our own table can never settle it: on the SMS branch
   `total_recipients` was written as the number ATTEMPTED, not delivered. Fixed
   going forward; the two historical rows are still only readable at Brevo.)
17. **Twenty-one contacts have a mixed-case email address and their unsubscribe
   link did nothing at all until 2026-09-12** (link 9, `docs/phase-4-campaign-automation.md`
   §11.1). Fixed — but **seven of the 21 are among the 44 frozen enrollments**,
   so they are the first people the sequencer reaches if item 3 above is
   answered "reschedule as-is", and they are the people most likely to press
   Spam rather than Unsubscribe because for them Unsubscribe visibly did
   nothing. Nothing to do technically; worth knowing before that call is made.
   The stored addresses were deliberately NOT rewritten to lowercase (a
   collision risk against `contacts_email_key`, and it would not help the 22nd
   address somebody types tomorrow) — the lookup is case-insensitive instead.
18. **The three reminder cron jobs have never been scheduled** (link 10,
   `docs/reminder-engine-review.md` §10). The whole reminder engine was
   *separately* dead until 2026-09-12 — `scheduled_reminders` had never held a
   row — and that is now fixed and proven in production. But nothing fires
   without cron-job.org: `/api/cron/send-reminders` every 15 min,
   `/api/cron/event-reminders` daily ~09:00 ET, `/api/cron/birthday-rebooking`
   weekly. **Order matters: `event-reminders` now ENQUEUES rather than sends, so
   scheduling it without `send-reminders` means event reminders still never go
   out.** Sixth, seventh and eighth jobs in the same cron-job.org backlog as
   items 1, 12 and 13 — one visit settles all of them.
19. **Whether to backfill reminders for the 18 real upcoming bookings** (link
   10). Turning `send-reminders` on does NOT retroactively mail anybody: the
   queue is empty and enqueue filters out any send time already in the past. But
   those 18 bookings had their reminders enqueued (and silently refused) when
   they were made, so they have nothing scheduled and will get no 7-day
   reminder, no balance chase, no day-of alert to Adam, no thank-you and no
   pre-arrival check-in text. Backfilling them would mail and text real
   customers about parties that are days away. The code is ready; the decision
   is a business one.

